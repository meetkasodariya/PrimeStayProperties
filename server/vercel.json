{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "build",
          "installCommand": "npm install --legacy-peer-deps"
        }
      },
      {
        "src": "server.js",
        "use": "@vercel/node",
        "config": {
          "installCommand": "npm install --legacy-peer-deps"
        }
      }
    ]
  }